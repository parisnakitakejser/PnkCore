apiVersion: v1
kind: Pod
metadata:
  name: web
  namespace: hubble-demo
  labels:
    app: web
spec:
  containers:
    - name: nginx
      image: nginx:1.27-alpine
      ports:
        - containerPort: 80
          name: http
